# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/claw.rb'

Hoe.new('claw', Claw::VERSION) do |p|
  # p.rubyforge_name = 'clawx' # if different than lowercase project name
  p.developer('James Coglan', 'jcoglan@googlemail.com')
  p.extra_deps = [['oyster', '>= 0.9']]
  p.extra_dev_deps = [['rspec', '>= 1.2'], ['cucumber', '>= 0.4']]
end

# vim: syntax=Ruby
